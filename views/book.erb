@book{<%= "#{@item.author}:#{@item.year}" %>, 
  author="<%= @item.author  %>",
  title="<%= @item.title %>",
  publisher="<%= @item.publisher %>",
  year="<%= @item.year %>",
  pages="<%= @item.pages %>",
}
